# Unified setup recovery verification

## Outcome

Recovered the existing implementation without weakening production permission checks. Verification used isolated temporary homes/targets only. No Google consent, credential import, account data reads/writes, live Hermes skill deployment, Git initialization/commit/push, SDD or review workflow was performed in this recovery pass.

## Commands and observed results

Run from the repository root:

| Command | Observed result |
|---|---|
| `npm test -- test/setup.test.ts` before correction | 3 passed, 2 failed: unsafe writable fixture `skills` directories |
| `npm test -- test/setup.test.ts` after correction | 6 passed |
| `npm run skills:sync` | 16 generated files updated; prior hashes matched, no edit conflicts |
| `npm run typecheck` | Exit 0 |
| `npm test` | 35 passed across 6 test files |
| `npm run build` | Exit 0 |
| `npm run skills:check` | 16 files checked, 0 updated |
| `npm run test:install` | Exit 0; real gws 0.22.5 installation verified |
| `npm run test:setup` final run | Exit 0; all three native discovery targets verified on disk |

Exact real-command outputs were retained in temporary verification reports printed by the scripts. Temporary evidence may be removed by operating-system cleanup; rerun the scripts to recreate current reports.

## Corrections and preserved behavior

- `test/setup.test.ts`: explicit 0700 fixture directories, exact injected-write rollback assertion and write-count check, independent 0770 rejection regression. The shell umask was 0002, explaining the original premature safety rejection.
- `scripts/verify-setup.mjs`: explicit private temporary homes, actual spaces and apostrophe in paths, exact validation of public Discovery cache schemas rather than an incorrect empty-config assertion. Initial real e2e failure was only this cache expectation; retained in a temporary verification report printed by the script.
- `README.md`: documented setup, official discovery paths, launcher lifetime, no-overwrite/idempotency/rollback/stale-lock behavior, verification commands and boundaries.
- `skills/`: synchronized four portable contracts and local runtime references from the existing `src/skills.ts` through hash-guarded generation; updated `scripts/skill-snapshots.json`. Ignored local `agent-config/skills/` copies are private runtime state and are not required by repository verification.
- Existing production `src/setup.ts`, `src/skills.ts`, CLI setup route and managed safety checks were retained, not rewritten.

The final setup e2e verifies 12 files per agent (four skills, four runtime references, four executable launchers), exact generated bytes/frontmatter, local reference existence, unchanged modification times on rerun, launchers from an unrelated cwd, service help, preserved user-edit conflict, and offline diagnostics. `doctor` reports installation verified, authentication missing and ready false. The Hermes sheets file in the retained test target intentionally contains a user-edit marker from the conflict-preservation test; Codex and Claude copies remain intact.

## Discovery evidence and limits

Official documentation checked during recovery:

- Hermes: https://hermes-agent.nousresearch.com/docs/user-guide/features/skills/ — selected `HERMES_HOME/skills`.
- Codex: https://developers.openai.com/codex/skills/ — user/project `.agents/skills`.
- Claude Code: https://code.claude.com/docs/en/skills — user/project `.claude/skills`.

A native path is not a running-host handshake. Agent loading, live OAuth, real formula evaluation against Google, write permissions, other agents and non-Linux-x64 execution remain unverified. Launchers depend on the recorded Node executable and built package paths staying in place. Service help can fetch public Discovery schemas but accesses no account resources in this test.

`skill_resolution: paths-injected`

Required full skills loaded from the delegated exact paths: skill-improver plus bundled style guide, productivity/google-workspace, and autonomous-ai-agents/hermes-agent. No global skill files or other Hermes profiles were modified.
